export default function HomePage() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: 24 }}>
      <h1>Call Center Backend</h1>
      <p>API service for the Expo mobile app.</p>
      <p>Use the REST endpoints under <code>/api</code> and connect to <code>/socket.io</code> for messaging events.</p>
    </main>
  );
}
